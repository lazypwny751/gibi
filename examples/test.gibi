ver="1.2.0"
repo="https://github.com/xd20111/inshackle"
entrypoint=(
    "file"                      # type of entry point
    "bash"                      # interpreter or execution method
    "${TOOLDIR}/inshackle.sh"   # exec file
)

# package : command
require_debian=(
    "coreutils:mkdir:mkdir"
    "coreutils:rm:rm"
    "coreutils:cp:cp"
    "grep:grep:grep"
    "curl:curl:curl"
)

require_ubuntu=(
    "coreutils:mkdir:mkdir"
    "coreutils:rm:rm"
    "coreutils:cp:cp"
    "grep:grep:grep"
    "curl:curl:curl"
)

require_kali=(
    "coreutils:mkdir:mkdir"
    "coreutils:rm:rm"
    "coreutils:cp:cp"
    "grep:grep:grep"
    "curl:curl:curl"
)

require_fedora=(
    "coreutils:mkdir:mkdir"
    "coreutils:rm:rm"
    "coreutils:cp:cp"
    "grep:grep:grep"
    "curl:curl:curl"
)

require_arch=(
    "coreutils:mkdir:mkdir"
    "coreutils:rm:rm"
    "coreutils:cp:cp"
    "grep:grep:grep"
    "curl:curl:curl"
)

# require_pisi=("") # çok yakında
